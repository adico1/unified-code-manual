# ROOT convergence — בלימה

```text
seed/ROOT.seed.json
→ verify every pinned semantic authority
→ resolve בלי_מה, registries, families and מה seeds
→ specialize seven stages
→ generate application source and tests
→ verify isolated execution and real GUI callbacks
→ rebuild independently
→ compare complete tree identity
→ בלימה
```

`ROOT.seed.json` pins every JSON semantic authority by identity, repository
relative path and canonical SHA-256. Duplicate identities, duplicate paths,
altered content, escaped paths, missing operation authorities, wrong canonical
states, a non-ten depth system or a different machine identity are rejected.

Projection convergence and root convergence are different. The current
operation proves deterministic convergence of the complete 65-application
projection. Whole-repository regeneration—including compiler, documentation
and physical host source—remains the final migration gate before this branch
replaces the older public implementation.
